<table>
  <tr><th>Roots</th><th>first</th><th>two</th><th>levels</th></tr>
  <tr>
      <% i = 0 %>
      <% @translations.sort{|key,value| key<=>value}.each do |tag , val | %>
        <td>
        <%=link_to tag , translation_url( tag ) %>
        </td>
        <%==  ((i+=1) % 4) == 0 ?  "</tr><tr>" : ""%>
      <%end%>
  </tr>
</table>
<table>
  <tr><th>Files</th><th></th><th></th><th></th></tr>
  <tr>    
  <% i = 0 %>
  <%    @files.each do |file , keys | %>
    <td><%=link_to file , file_translation_url(file) %>     </td>
    <%==  ((i+=1) % 4) == 0 ?  "</tr><tr>" : ""%>
  <%end%>
</table>

<%= render :partial => "footer" %>